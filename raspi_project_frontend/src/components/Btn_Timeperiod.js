import React from "react"

const Btn_Timeperiod = ({ onClickHandler }) => {
    return (
        <div>
            <button onClick={onClickHandler}>Past 7 days</button>
        </div>
    )
}

export default Btn_Timeperiod